{
  "name": "pre-signup-trigger",
  "version": "1.0.0",
  "description": "Cognito Pre Sign-Up Lambda trigger for email domain validation",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-sns": "^3.987.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
